export const ROOM_LIMIT = 3;
export const PARENT_CATEGORY_ID = '881071692414345250';
export const ROOM_NAME = ['Chane', 'Tane', 'Rain']; // Keep all the names in capitalized case, can also give space
// To Do: Do this so first letter is Capital and if any space exist or not?, just keep them in proper
export const DEFAULT_ROOM = 'Tane';
// SOme problem with creating the default room, it should already exist, no?
